# Frontend build-time configuration.
# The Angular app reads its API base URL from this env var at BUILD time
# (see scripts/set-env.mjs, run by the "prebuild" npm hook). On Vercel, set
# API_BASE_URL in the project's environment variables to the Render backend URL.
API_BASE_URL=http://localhost:8080
